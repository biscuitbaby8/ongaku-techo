# AdSense 再不承認（2026-08）の原因調査

調査日: 2026-08-20 / 対象: 本番 `https://ongakutecho.com`（デプロイ 2026-08-19 19:56 時点）
前回調査: `docs/adsense-improvement-plan.md`（2026-07-26 作成 / 07-29 追記）

---

## 結論

**前提が間違っていた。** 前回ドキュメントは「Phase 2（コンテンツの作り直し）は完了した。
全1,116語に1,100〜2,100文字の記事を書いた」としているが、`src/data/termArticles.js` を
実測すると **1記事あたりの独自本文は平均463文字・中央値450文字** で、
主張されている量の 1/3 以下しかない。

つまり前回「解決済み」とされた **2番目の原因（低品質・大量生成コンテンツ）は
解消されていない**。今回の不承認は、前回と同じ理由で落ちていると考えるのが自然。

「再クロールが追いついていないだけ」という前回の見立ては、この実測により否定される。
待っても状況は変わらない。

---

## 1. 【最重要】記事の分量が主張の 1/3 以下

`termArticles.js` の全1,116エントリについて、`lead` + `sections`(見出し+本文) +
`confusions` + `instruments` + `works` の文字数を合計した実測値。

| 指標 | 実測値 | ドキュメントの主張 |
|---|---|---|
| 平均 | **463文字** | 1,100〜2,100文字 |
| 中央値 | **450文字** | 〃 |
| 最短 | **134文字** | 〃 |
| 最長 | 1,134文字 | 〃 |

分布:

| 閾値 | 該当件数 | 割合 |
|---|---|---|
| 300文字未満 | 248件 | 22.2% |
| 400文字未満 | 473件 | 42.4% |
| 600文字未満 | 846件 | 75.8% |
| 800文字未満 | 1,063件 | **95.3%** |
| 1,100文字未満 | 1,115件 | 99.9% |

最短の15件（いずれも他言語の同義語ページ）:

```
mäßig:134  ruhig:137  blech:144  take:146  bewegt:147  animé:147
tres-vite:147  etwas-bewegt:148  belebt:151  assez-vite:152  ballo:152
più:153  en-pressant:154  accellerato:156  comodamente:156
```

### なぜ「1,100〜2,100文字」という数字が出たのか

おそらく**ページ全体の可視テキスト**（ヘッダー・パンくず・関連用語8件・フッター等の
全ページ共通部分を含む）を数えている。ただしそれで数えても主張には届かない。
`dist/term/*/index.html` から `<script>` `<style>` `<head>` を除いて可視テキストを実測すると:

| 指標 | 実測値 |
|---|---|
| ページ全体の可視テキスト 中央値 | **925文字** |
| 同 最短 | 579文字 |
| 同 最長 | 1,634文字 |

つまり **ページ全体を数えても中央値925文字**で、そのうち約半分は全ページ共通の定型部分。
「各ページ1,100〜2,100文字超」という記述はどちらの数え方でも成立しない。

### 「厚い記事」のテンプレートが適用されているのは一部だけ

`/term/adagio/` のような、語源・演奏のヒント（楽器別）・混同しやすい用語・使われている曲まで
揃った構成は例外。フィールドの保有率は次のとおり。

| フィールド | 保有記事数 / 1,116 |
|---|---|
| `lead` | 1,116 (100%) |
| `sections` | 1,116 (100%) — ただし **1つ以下が248件** |
| `confusions` | 1,059 (95%) |
| `works`（使われている曲） | **175 (15.7%)** |
| `instruments`（楽器別のヒント） | **161 (14.4%)** |

**演奏のヒントも曲例もどちらも無い記事が 941件（84%）。**
`/term/adagio/` を見て「全ページこの厚さ」と判断すると実態を大きく取り違える。

### カテゴリ別

| カテゴリ | 平均文字数 | 語数 | うち300字未満 |
|---|---|---|---|
| 強弱 | 652字 | 40語 | 0語 |
| 理論 | 573字 | 88語 | 2語 |
| 形式 | 525字 | 33語 | 3語 |
| 楽器 | 505字 | 39語 | 4語 |
| 構成 | 474字 | 277語 | 67語 |
| 奏法 | 451字 | 223語 | 45語 |
| 速度 | 412字 | 101語 | **54語** |
| 表情 | 412字 | 315語 | **73語** |

語数の多い「表情」「構成」「奏法」「速度」（計916語＝全体の82%）が、そのまま薄い側に集中している。

---

## 2. 同義語ページが残っている

同一の `meaning` を共有する用語が **31グループ・68語** 残存している。
前回調査時点（35グループ・75件）からほぼ変わっていない。

```
「だんだん弱く」   → Decrescendo, Diminuendo, Abnehmend
「消えゆくように」 → Smorzando, Perdendosi, Diluendo
「少し、わずかに」 → Poco, Wenig, Un poco
「表情豊かに」     → Espressivo, Mit Ausdruck, Expressif
「だんだん遅く」   → Ritardando, Rallentando
「速く」           → Schnell, Geschwind
「遅く」           → Langsam, Lent
...
```

上の「最短15件」と見比べると分かるとおり、**最も薄いページ群 = この同義語・他言語訳語の
ページ群**。イタリア語版に本文を書き、ドイツ語版・フランス語版は「◯◯のドイツ語」で
1ページ立てる、という構造が残っている。

本文レベルの近似も検出した（4-gram Jaccard > 0.4 のペアが15組）:

```
angosciamento / angoscioso  0.66
furieusement  / furioso     0.65
accarezzevole / carezzevole 0.58
abbandonatamente / con-abbandono 0.57
dampfer-auf   / dampfer-ab  0.56
amabilmente   / amabile     0.54
```

完全一致する本文パラグラフも10種類20箇所ある。

---

## 3. カテゴリ一覧ページに独自の文章が1文字も無い

`/index/` および `/index/{カテゴリ}/` の9ページは、用語カードを並べただけのリンク集。
導入文・解説文が一切無い。前回ドキュメントの Phase 2 案B-3
（カテゴリページに2,000字程度の導入解説を追加）は、案Bを採らなかったため未実施のまま。

sitemap に載っている1,129 URLの内訳は次のとおりで、**辞書エントリとリンク集以外のページが
1枚も無い**。

| 種別 | 枚数 |
|---|---|
| 用語ページ | 1,116 |
| カテゴリ一覧（リンク集） | 9 |
| トップ | 1 |
| about / contact / privacy | 3 |

前回ドキュメントが挙げていた「辞書以外のオリジナル記事を5〜10本追加する」も未着手。
**サイト全体が「辞書エントリ＋ツール」だけで、読み物としての独自コンテンツがゼロ**という
状態になっている。

---

## 4. 「大量生成コンテンツ」のパターンに合致している

Google は2024年3月のスパムポリシー改定で
「**大規模に生成されたコンテンツの不正使用**（scaled content abuse）」を明文化した。
判断されるのは「AIを使ったかどうか」ではなく「独自の価値が薄いページを大量に作っているか」。

現状は次のとおり、そのパターンに正面から当たる。

- 1,116ページが**完全に同一のテンプレート**（意味 → 解説 → セクション → 関連用語8件 → フッター）
- 独自本文は中央値450文字、22%が300文字未満
- 見出しも定型が多い（「語源」74回、「演奏上の要点」68回、「演奏上の判断」65回）
- 全ページが短期間に一括で公開されている（git履歴: 2026-07-27〜07-29 の数日で約1,100語分）
- **過去にクローキング（スパムポリシー違反）を実装していた履歴がある**
  （`61dd33d` / `60d2303`。前回調査で撤去済みだが、サイトの信頼度に影響している可能性は残る）

用語数1,116という規模は、この文脈では加点ではなく**減点方向**に働く。

---

## 5. 運営者情報の裏付けが無い

`public/about.html` に次の記述がある。

> 現役の奏者・講師の経験に基づいた「演奏現場で役立つ実用的な解説」を心がけています。
> ※このアプリは、現役の奏者や講師の監修を元に、biscuitbaby / ongaku-techo Project が運営しています。

トップページの `#seo-content` にも「現役の奏者や講師の監修を元に制作されています」とある。
しかし**執筆者・監修者の氏名も経歴も、サイトのどこにも書かれていない**。
審査側から見ると検証できない主張であり、E-E-A-T（専門性・権威性・信頼性）の観点では
むしろマイナスに働く。

- 実際に監修者がいるなら → 氏名・経歴・担当範囲を About に明記する
- いないなら → 記述を実態に合わせて書き換える（「独自に執筆」で十分）

あわせて About の「強弱、速度、奏法、表情、構成の5カテゴリ」という記述も誤り。
実際は形式・理論・楽器を含む**8カテゴリ**。

---

## 6. その他の減点材料

| 項目 | 状態 |
|---|---|
| ヘッダーに `SEARCH OPTIMIZED V8.0` と表示（審査員に見える位置でSEO目的を明示） | 要修正 |
| JSバンドルが 610KB → **1.86MB** に増加（Firebase移行の影響）。モバイルの表示速度・Core Web Vitals に直撃 | 要対応 |
| `AdSlot` の `SHOW_ADS` が `false`（`src/App.jsx:80`）。審査中は問題ないが、承認後に有効化が必要 | 承認後 |
| 404ページが Vercel の素のテキスト表示 | 任意 |

---

## 今回の調査で「問題なし」と確認できたもの

前回の指摘のうち、以下は本番で実際に確認して**解消済み**だった。これらは再申請の障害ではない。

| 項目 | 確認方法 | 結果 |
|---|---|---|
| クローキング（`#seo-content` 削除スクリプト）の再発 | 本番HTML取得 + ヘッドレスChromiumで描画後のDOM確認 | ✅ 再発なし。`#seo-content` は `#root` の後ろにあり、描画後も残る |
| 重複Vercelプロジェクト | Vercel API でプロジェクト一覧を取得 | ✅ `ongaku-techo` のみ。m8lz / wm8h は削除済み |
| `*.vercel.app` の noindex | 本番・vercel.app 両方のレスポンスヘッダを比較 | ✅ vercel.app のみ `x-robots-tag: noindex, nofollow`。本番には付いていない |
| sitemap の404URL | 1,129 URL すべてを `dist/` の生成物と突き合わせ | ✅ 欠損0件。逆に未掲載の用語ページも0件 |
| ads.txt | 内容確認 | ✅ `google.com, pub-2953839366795600, DIRECT, f08c47fec0942fa0` |
| AdSenseコードの設置 | index.html / about / contact / privacy / 生成される全用語ページ | ✅ 全ページに設置済み |
| プライバシーポリシーの実態整合 | Firebase・Gemini API・Vercel Analytics の記載を確認 | ✅ 8/19のFirebase移行も反映済み |
| お問い合わせ手段 | contact.html | ✅ メール + Google Forms + 運営者情報あり |
| SPAとクローラーの内容差 | ヘッドレスChromiumで描画（console error なし、`#root` に1,938文字） | ✅ 用語モーダルから `/term/{slug}/` へのリンクあり。差は無い |
| ビルド | `npm ci && npm run build` | ✅ 成功（1,116用語 + 9一覧ページ生成） |

---

---

## 実施した対応（2026-08-20）

下記「提案」の優先度1〜3のうち、コードで対応できるものを実装した。

### 構造：1,116ページ → 178ページ

`scripts/article-quality.js` に個別ページの品質基準を定義し、これを満たす用語だけが
`/term/{slug}/` を持つようにした。

> 語源・楽器別の演奏のヒント・混同しやすい用語・使われている曲がすべて揃っていて、
> 独自本文が500文字以上あること

該当は **161語**。残り955語は個別ページを廃止し、カテゴリ一覧ページ内に
`意味 + 解説` つきの項目（`id="{slug}"`）として掲載した。
旧URLは `api/term-redirect.js` が `/index/{カテゴリ}/#{slug}` へ **301** で転送する。
存在しない用語は301せずに404を返す（実在しないURLの恒久リダイレクトはソフト404扱いになるため）。

対象を `vercel.json` の `redirects`（上限1024件）に書き出さずサーバー関数にしたのは、
用語が増減するたびに設定を作り直す運用にしないため。Vercel はファイルシステムを
`rewrites` より先に評価するので、個別ページのある用語はこの関数に到達しない。

判定結果は `src/data/featuredTerms.js`（自動生成・コミット対象）に書き出している。
記事を書き足したら `npm run featured` で再生成する。内容が古いまま、あるいは基準を
下回るページが混ざっているとビルドが失敗する。

### 各ページの分量（実測・ビルド後のHTMLから可視テキストを計測）

| ページ種別 | 枚数 | 中央値 | 最小 | 最大 |
|---|---|---|---|---|
| 用語の個別ページ | 161 | **1,231字** | 970字 | 1,630字 |
| カテゴリ一覧（解説つき） | 8 | **10,668字** | 3,545字 | 25,077字 |
| 読み物記事 | 6 | **1,604字** | 1,292字 | 1,859字 |
| 用語さくいん `/index/` | 1 | 36,572字 | — | — |
| トップ | 1 | 3,218字 | — | — |

対応前は「1,116枚・中央値925字（うち独自本文450字）」だった。

### 新しく書いたコンテンツ

- **カテゴリ解説 9本・約11,000字**（`src/data/categoryIntros.js`）
  一覧ページが単なるリンク集だった問題への対応。「強弱記号は音量つまみではない」
  「速度用語は速さより性格を指している」など、そのカテゴリの用語をどう読むかを解説する。
- **読み物記事 6本・約7,800字**（`src/data/guideArticles.js` → `/guide/`）
  辞書項目と内容が重複しないよう、テーマを「用語や道具を練習でどう使うか」に限定した。
  メトロノーム練習の設計 / チューナーのセント表示の読み方 / 速度用語とBPMの対応 /
  譜読みの最初の10分 / 強弱が書かれていない楽譜 / 練習記録のつけ方。

### 記述の修正

- About・トップ・アプリ内の「現役の奏者や講師の監修を元に」という記述を削除し、
  「運営者自身が執筆している」「誤りの指摘を受け付ける」という実態に沿った記述に変更
- About の「5カテゴリ」→ 実際の8カテゴリに修正
- ヘッダーの `SEARCH OPTIMIZED V8.0` を削除
- privacy.html にフッターナビゲーションを追加（他ページから孤立していた）

### 技術面

- 初回JSバンドル **1.86MB → 1.13MB**。アプリ側が `termArticles.js`（1.8MB）を
  読み込まなくなったため（必要なのは「個別ページがあるか」の判定だけなので、
  自動生成したスラッグ一覧を参照する形に変更）。本番では静的HTMLに覆われて
  到達しない `/term/*` `/index/*` のReactルートは遅延読み込みに変更した。
- sitemap は 1,129 URL → **181 URL**（リダイレクトされるURLを申告しない）
- ビルド時チェックを追加。スラッグ重複・未登録カテゴリ・カテゴリ解説の欠落・
  記事のリンク切れ・`featuredTerms.js` の陳腐化・基準を下回る個別ページ・
  読み物の参照先の不在を検出したらビルドを失敗させる。

### 検証済み

- `npm ci && npm run build` 成功（178ページ生成）
- dist内の内部リンク **リンク切れ0**、`#slug` アンカーの **未定義0**
- sitemap 181 URL すべてに生成物が存在、生成物の未掲載も0
- リダイレクト関数の単体確認（廃止用語→301 / 個別ページあり→302・ループガード付き /
  存在しない用語→404 / 非ASCIIスラッグの Location ヘッダーがASCIIであること）
- ヘッドレスChromiumでトップを描画（エラーなし、`#seo-content` は削除されず残存）、
  用語モーダルのリンク先が個別ページ／カテゴリアンカーで正しく切り替わることを確認

### 残っている作業（コードでは対応できないもの）

1. **Search Console で sitemap を再送信**する（URL数が1,129→181に変わったため）
2. 主要ページ（トップ・`/index/`・各カテゴリ・`/guide/` 各記事）のインデックス登録をリクエスト
3. 実際に監修者がいるなら、About に**氏名・経歴・担当範囲を明記**する
   （いないなら現在の記述のままでよい）
4. **2週間以上あけてから再申請**する。再申請前に必ず AdSense の
   「サイト」「ポリシーセンター」に具体的な違反種別が出ていないか確認する

---

## 提案：次に何をするか

前回ドキュメントは「案A: 全ページを厚くする」を選んで実行したが、実測のとおり目標量に
届いていない。全1,116ページを800〜1,000字にするには残り40〜60万字の執筆が必要で、
やはり現実的ではない。**前回ドキュメント自身が推奨していた案B（ページを絞る）に切り替えるのが妥当。**

### 優先度1 — ページ数を絞る

1. `termsData.js` に `isFeatured: true` を持たせ、**主要150〜200語だけ**個別ページを生成する
   （`scripts/generate-static-pages.js` を1行分岐させるだけで実装できる）
   - 残す語の選定基準: 独自本文が長い順 + 検索需要のある基本用語
   - 現状 `works` と `instruments` の両方を持つ161語がほぼそのまま候補になる
2. 残り約900語は個別ページを廃止し、カテゴリ一覧ページ内の項目に格下げ。
   既存URLは `vercel.json` の `redirects` でカテゴリ一覧へ301リダイレクト
3. 残した150〜200語は、`/term/adagio/` と同じ厚さ（語源・楽器別のヒント・混同しやすい用語・
   使われている曲）まで揃える

### 優先度2 — 辞書以外のコンテンツを作る

4. カテゴリ一覧9ページに、そのカテゴリの全体像を解説する導入文（各1,500〜2,000字）を追加
5. 読み物としてのオリジナル記事を5〜10本追加（`/article/` 等）。例:
   - 強弱記号は「音量」ではない — p と pp の本当の違い
   - Andante と Adagio と Lento を混同しないための3つの基準
   - メトロノーム練習でやってはいけないこと
   - チューナーのセント表示の読み方
   - 速度用語をBPMに読み替える早見表

### 優先度3 — 信頼性・技術面

6. About の監修者記述を実態に合わせる（氏名・経歴を出すか、記述を外すか）。カテゴリ数の誤記も修正
7. ヘッダーの `SEARCH OPTIMIZED V8.0` を外す
8. `termsData.js` / `termArticles.js` を動的importに分割し、初回バンドル1.86MBを削減

### 再申請のタイミング

上記の優先度1・2を反映してデプロイ → Search Console でsitemap再送信とインデックス登録を
リクエスト → **2週間以上あけてから**再申請する。

**再申請の前に必ず AdSense の「サイト」「ポリシーセンター」を確認すること。**
具体的な違反種別が表示されていれば、この文書の推測よりそちらを優先する。

---

## 調査方法（再現手順）

```bash
npm ci && npm run build          # dist/ に1,116用語ページを生成

# 記事の文字数分布
node -e "import('./src/data/termArticles.js').then(m=>{ /* lead+sections+confusions+instruments+works を合計 */ })"

# ページ全体の可視テキスト
# dist/term/*/index.html から script/style/head を除去してタグを剥がし文字数を測る

# 描画後のDOM確認（クローキング検証）
npx vite preview --outDir dist    # + playwright-core / /opt/pw-browsers/chromium
```

本番HTMLの取得はネットワークポリシーにより直接HTTPできないため、Vercel API 経由で行った。
